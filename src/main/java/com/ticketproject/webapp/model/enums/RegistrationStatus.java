package com.ticketproject.webapp.model.enums;

/**
 * RegistrationStatus is an enum representing whether
 * people can still register for a public event.
 */
public enum RegistrationStatus
{
    OPEN,
    CLOSED
}