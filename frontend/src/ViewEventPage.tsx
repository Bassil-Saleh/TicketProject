/**
 * ViewEventPage is for implementing the page that lets a user
 * view an event's page given the event's public ID.
 * @returns JSX for the site's view event page
 */
export function ViewEventPage() {
    // TODO: Add CSS styling.

    // TODO: Add logic to retrieve and display an
    //       event's info using the GET API Route
    //       to fetch an event's info as reference.

    // TODO: If the fetched event is a public event:
    //       - Show a "Register" button to let the
    //       user register for the event.
    //       - If the event's registration status is
    //       closed, then disable the "Register" button
    //       and show a tooltip saying registration is
    //       closed when the user hovers their cursor
    //       over the button.

    // TODO: If the fetched event is a private event:
    //       - Do not show a "Register" button.

    // TODO: If an event public ID is provided that does
    //       not correspond to any existing event,
    //       show an appropriate message.

    return (
        <h1>Event Title Goes Here</h1>
    );
}