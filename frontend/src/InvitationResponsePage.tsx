/**
 * InvitationResponsePage is for implementing the page which
 * someone is sent to when they either click "Accept Invitation"
 * or "Reject Invitation" in the email they received when an
 * event host invites them to a private event.
 * @returns JSX for the site's invitation response page.
 */
export function InvitationResponsePage() {
    // TODO: Add a form that lets the user:
    //       - Pick a response (Accepted or Rejected)
    //       - Type an optional message to the event host
    //         to include with their response

    // TODO: Extract the "publicToken" URL query and include it
    //       along with the optional message in a JSON request body
    //       to the API route PATCH /api/v1/tickets/invitation.

    // TODO: Show an appropriate message to the user based on the
    //       result of the request (success, failure, etc.).

    // TODO: If the "publicToken" URL query is missing, show an
    //       error message to the user.

    return (
        <div></div>
    );
}