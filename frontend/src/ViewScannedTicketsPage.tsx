/**
 * ViewScannedTicketsPage is for implementing the page
 * that lets a logged in event host view a list of tickets
 * they've scanned ever since they first created their account.
 * @returns JSX for the site's view scanned tickets page
 */
export function ViewScannedTicketsPage() {
    // TODO: Add CSS styling.

    // TODO: If the user visits this page but they are
    //       not currently logged in, then they should
    //       be automatically redirected to the home page.

    // TODO: Add logic to fetch the list of tickets scanned
    //       by the logged in event host, using the GET API
    //       route to fetch ticket scans as reference.

    return (
        <h1>Tickets You've Scanned</h1>
    );
}