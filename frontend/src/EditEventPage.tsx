/**
 * EditEventPage is for implementing the page that lets a
 * logged in event host edit or delete an event which
 * they've already created.
 * @returns JSX for the site's edit event page
 */
export function EditEventPage() {
    // TODO: Add CSS styling.

    // TODO: If the user visits this page but they are
    //       not currently logged in, then they should
    //       be automatically redirected to the home page.

    // TODO: Add logic to let a logged in event host edit
    //       an event they've created by using the PATCH
    //       API routes for editing an event's details
    //       as reference.

    // TODO: Add a form to let the logged in event host
    //       edit an event they've already created.

    // TODO: Add logic to let a logged in event host delete
    //       an event they've created by using the DELETE API
    //       route for deleting an event as reference.

    // TODO: Show an appropriate message after receiving a
    //       response from the backend when attempting to
    //       edit an event (success, failure, etc.).

    // TODO: Add a "Delete Event" button to let a logged in
    //       event host delete an event they've created.

    // TODO: Show a confirmation dialog when the user clicks
    //       the "Delete Event" button.

    // TODO: Show an appropriate message after receiving a
    //       response from the backend when attempting to
    //       delete an event (success, failure, etc.).

    return (
        <h1>Edit Event</h1>
    );
}