/**
 * CreateEventPage is for implementing the page that lets
 * a logged in event host create a new event.
 * @returns JSX for the site's create event page
 */
export function CreateEventPage() {
    // TODO: Add CSS styling.

    // TODO: If the user visits this page but they are
    //       not currently logged in, then they should
    //       be automatically redirected to the home page.

    // TODO: Add a form and logic to let the user create
    //       an event using the POST API route to create
    //       a new event as reference.

    // TODO: Show an appropriate message to the user based
    //       on the result of the sent request to create
    //       a new event (success, failure, etc.).

    // TODO: Add a cancel button to let the user go back
    //       to the dashboard page.

    return (
        <h1>Create Event</h1>
    );
}