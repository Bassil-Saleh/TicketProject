/**
 * CreateInvitationPage is for implementing the page that lets
 * a logged in event host invite someone to a private event
 * they've created.
 * @returns JSX for the site's invitation creation page.
 */
export function CreateInvitationPage() {
    // TODO: If the user is not currently logged in, then automatically
    //       redirect them to the home page.

    // TODO: If the user is currently logged in, then display a form
    //       to let them input:
    //       - The invitee's first name.
    //       - The invitee's middle name (optional).
    //       - The invitee's last name.
    //       - The invitee's email address.
    //       - A "Send Invitation" button.
    //       Also display a button to go back to the dashboard.

    // TODO: When the user clicks "Send Invitation", send a request
    //       to the API route POST /api/v1/attendees/invitation
    //       using the form's input.

    // TODO: Show an appropriate message to the user based on the
    //       result of the sent request (success, failure, etc.).

    return (
        <div></div>
    );
}