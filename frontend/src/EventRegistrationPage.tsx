/**
 * EventRegistrationPage is for implementing the page
 * that lets a user register for a public event identified
 * by the event's public ID. Note that a user does not need
 * to be logged in to access or use this page.
 * @returns JSX for the site's public event registration page
 */
export function EventRegistrationPage() {
    // TODO: Add CSS styling.

    // TODO: Add logic to let a user register for a
    //       public event using the POST API route
    //       for creating a public event registration
    //       as reference.

    // TODO: Add logic to fetch an event given its
    //       public ID as an HTTP GET parameter
    //       using the GET API route for fetching
    //       an event's info as reference.

    // TODO: If the fetched event is a private event:
    //       - Do not show a registration form.
    //       - Show a message saying that if the user
    //         wants to attend the event, the event
    //         host who created the event would have
    //         to send them an invitation.

    // TODO: Add a form to let a user input their info
    //       to register for the event.

    // TODO: Show an appropriate message based on the
    //       result of the event registration request
    //       (success, failure, etc.).

    return (
        <h1>Registration</h1>
    );
}