# Serves the built React frontend as static files with SPA fallback routing
# so that client-side routes (react-router) resolve on refresh / deep links.
server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
